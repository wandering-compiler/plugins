// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: worker.proto

// The protocol between a RELAY and a WORKER.
//
// # Why this is not one of the plugin's protos
//
// Everything under `plugins/cluster/proto/` is materialised into the consuming
// project: tables, services the project serves, clients it calls. This
// conversation has neither end there. A relay is a separate process and a
// worker is somebody's laptop; the project never speaks it and would have to
// generate — and register — a facade for a service it has nothing to do with.
//
// So it is a plain gRPC contract with no w17 annotations, generated by buf into
// this package and compiled into the relay and the worker SDK, which are the
// only two programs that know it exists.

package workerpb

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	WorkerAttach_Attach_FullMethodName = "/w17.cluster.worker.WorkerAttach/Attach"
)

// WorkerAttachClient is the client API for WorkerAttach service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// WorkerAttach is how a machine offers itself to a relay.
type WorkerAttachClient interface {
	// Attach announces a worker and stays open for as long as it is available.
	//
	// The connection IS the availability: a relay knows a worker is there
	// because the stream is open, and knows it is gone when the stream ends.
	// Nothing polls, nothing heartbeats, and there is no liveness table to
	// reconcile — the same reason a scheduling stream holds a queue place.
	//
	// The worker's IDENTITY is not in this conversation. It is the fingerprint
	// of the certificate it presented in the TLS handshake, which is the one
	// thing here it proved rather than asserted; everything a worker SENDS is a
	// claim, carried for an operator to read and for scheduling to use.
	//
	// A DENIED worker gets PermissionDenied and the stream never opens.
	//
	// BIDIRECTIONAL since 2026-09-25, and the reason is the difference between
	// "connected" and "able to work". A one-shot announcement could only be
	// contradicted by hanging up, so the sole way to stop receiving work was to
	// drop the tunnel — which kills the sessions still running on it. A browser
	// pool cannot be deployed that way, and a hung engine on a healthy socket
	// kept being handed work with nothing able to say otherwise (marb,
	// 2026-09-25).
	Attach(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[WorkerMessage, AttachEvent], error)
}

type workerAttachClient struct {
	cc grpc.ClientConnInterface
}

func NewWorkerAttachClient(cc grpc.ClientConnInterface) WorkerAttachClient {
	return &workerAttachClient{cc}
}

func (c *workerAttachClient) Attach(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[WorkerMessage, AttachEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &WorkerAttach_ServiceDesc.Streams[0], WorkerAttach_Attach_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[WorkerMessage, AttachEvent]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WorkerAttach_AttachClient = grpc.BidiStreamingClient[WorkerMessage, AttachEvent]

// WorkerAttachServer is the server API for WorkerAttach service.
// All implementations must embed UnimplementedWorkerAttachServer
// for forward compatibility.
//
// WorkerAttach is how a machine offers itself to a relay.
type WorkerAttachServer interface {
	// Attach announces a worker and stays open for as long as it is available.
	//
	// The connection IS the availability: a relay knows a worker is there
	// because the stream is open, and knows it is gone when the stream ends.
	// Nothing polls, nothing heartbeats, and there is no liveness table to
	// reconcile — the same reason a scheduling stream holds a queue place.
	//
	// The worker's IDENTITY is not in this conversation. It is the fingerprint
	// of the certificate it presented in the TLS handshake, which is the one
	// thing here it proved rather than asserted; everything a worker SENDS is a
	// claim, carried for an operator to read and for scheduling to use.
	//
	// A DENIED worker gets PermissionDenied and the stream never opens.
	//
	// BIDIRECTIONAL since 2026-09-25, and the reason is the difference between
	// "connected" and "able to work". A one-shot announcement could only be
	// contradicted by hanging up, so the sole way to stop receiving work was to
	// drop the tunnel — which kills the sessions still running on it. A browser
	// pool cannot be deployed that way, and a hung engine on a healthy socket
	// kept being handed work with nothing able to say otherwise (marb,
	// 2026-09-25).
	Attach(grpc.BidiStreamingServer[WorkerMessage, AttachEvent]) error
	mustEmbedUnimplementedWorkerAttachServer()
}

// UnimplementedWorkerAttachServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedWorkerAttachServer struct{}

func (UnimplementedWorkerAttachServer) Attach(grpc.BidiStreamingServer[WorkerMessage, AttachEvent]) error {
	return status.Error(codes.Unimplemented, "method Attach not implemented")
}
func (UnimplementedWorkerAttachServer) mustEmbedUnimplementedWorkerAttachServer() {}
func (UnimplementedWorkerAttachServer) testEmbeddedByValue()                      {}

// UnsafeWorkerAttachServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to WorkerAttachServer will
// result in compilation errors.
type UnsafeWorkerAttachServer interface {
	mustEmbedUnimplementedWorkerAttachServer()
}

func RegisterWorkerAttachServer(s grpc.ServiceRegistrar, srv WorkerAttachServer) {
	// If the following call panics, it indicates UnimplementedWorkerAttachServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&WorkerAttach_ServiceDesc, srv)
}

func _WorkerAttach_Attach_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(WorkerAttachServer).Attach(&grpc.GenericServerStream[WorkerMessage, AttachEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WorkerAttach_AttachServer = grpc.BidiStreamingServer[WorkerMessage, AttachEvent]

// WorkerAttach_ServiceDesc is the grpc.ServiceDesc for WorkerAttach service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var WorkerAttach_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "w17.cluster.worker.WorkerAttach",
	HandlerType: (*WorkerAttachServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Attach",
			Handler:       _WorkerAttach_Attach_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "worker.proto",
}
